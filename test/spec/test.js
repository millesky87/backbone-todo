/* global describe, it */

(function () {
    'use strict';

    describe('On page load', function () {
            it('should exist in the DOM',function (done) {
              $(document).ready(function(){
                expect()
              }

            });
        });
    });
})();
